apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "task6-chart.fullname" . }}
  labels:
    {{- include "task6-chart.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "task6-chart.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "task6-chart.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: nginx
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 80
              protocol: TCP
          resources:
            {{- toYaml .Values.resources | nindent 12 }}