apiVersion: v1
kind: Service
metadata:
  name: {{ include "task6-chart.fullname" . }}
  labels:
    {{- include "task6-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "task6-chart.selectorLabels" . | nindent 4 }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
      protocol: TCP
      name: http